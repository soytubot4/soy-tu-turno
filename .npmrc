# Workspace-wide pnpm config.
#
# `production=false` fuerza a pnpm a instalar TODAS las deps (dev incluidas)
# durante `pnpm install`, sin importar que NODE_ENV=production esté seteado.
# Necesario para builds en DO App Platform (y similares) que setean
# NODE_ENV=production por default.
production=false
